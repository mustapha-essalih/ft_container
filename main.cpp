#include <iostream>
#include <algorithm>
#include <utility>
#include <memory>
#include <stack>
#include <vector>
 



using std::string;
using std::cout;
using std::endl;
using std::vector;
using std::stack;
  
#include "map.hpp"

int main()
{

    ft::map<int,int> m;
    
    // m.insert(ft::make_pair<int,int>(10,100));
    // m.insert(ft::make_pair<int,int>(20,200));
    // m.insert(ft::make_pair<int,int>(30,300));
    
    // // ft::map<int,int>::iterator it =m.begin();
    
    // for (ft::map<int,int>::iterator it =m.begin(); it!=m.end(); ++it)
    //     std::cout << it->first << " => " << it->second << '\n';
 
}
 